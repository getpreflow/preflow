<!DOCTYPE html>
<html lang="en">
<head><meta charset="utf-8"><title>{{ record.title }}</title></head>
<body>
    <article>
        <h1>{{ record.title }}</h1>
        <div>{{ rendered.body|default('')|raw }}</div>
    </article>
</body>
</html>
